var interventions = <%= raw(@graph.interventions.to_json) %>;
var graph = <%= raw(@graph.to_json) %>
$("#intervention-form form").trigger("reset");
$("#intervention-form").slideToggle();

DVE.current_graph.add_intervention(interventions)

DVE.current_graph.update_sidebar(interventions)

